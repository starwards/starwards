declare module '@colyseus/monitor' {
    // import express from 'express';
    // import { Server } from "colyseus";
    // export declare function monitor(server: Server): express.Router;
    export declare function monitor(server: any): any;
}