{
  "name": "@starwards/test-kit",
  "version": "0.0.0",
  "main": "target/index.js",
  "types": "target/index.d.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf ./target",
    "prebuild": "yarn clean",
    "build": "ts-build ./src --cjs",
    "lint": "tslint -p ./",
    "test": "mocha -r @ts-tools/node/warn test/**/*.spec.ts --watch-extensions ts --colors",
    "start": "nodemon -r @ts-tools/node/r ./src/main.ts",
    "prepack": "yarn build"
  },
  "files": [
    "target"
  ],
  "peerDependencies": {
    "chai": "~4.2.0",
    "tslib": "~1.9.3"
  },
  "dependencies": {
    "sinon": "~7.3.2"
  }
}
