scalar Vector

interface SpaceObject {
  id: ID!
  position: Vector!
}

type Asteroid implements SpaceObject {
  id: ID!
  position: Vector!
}

type Query {
  allObjects: [SpaceObject!]!
  objectsInRadius(position: Vector!, radius: Float!): [SpaceObject!]!
}
