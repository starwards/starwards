{
  "name": "@starwards/model",
  "version": "0.0.0",
  "main": "cjs/index.js",
  "types": "cjs/index.d.ts",
  "files": [
    "cjs"
  ],
  "license": "MIT",
  "scripts": {
    "clean": "rimraf ./cjs",
    "prebuild": "yarn clean",
    "build": "ts-build ./src --cjs",
    "lint": "tslint -p src",
    "test": ";",
    "prepack": "yarn build"
  },
  "dependencies": {
    "@colyseus/schema": "^0.4.23"
  }
}
