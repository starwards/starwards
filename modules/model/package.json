{
    "name": "@starwards/model",
    "version": "0.0.0",
    "main": "cjs/index.js",
    "types": "cjs/index.d.ts",
    "files": [
        "cjs"
    ],
    "scripts": {
        "clean": "rimraf ./cjs",
        "prebuild": "yarn clean",
        "build": "ts-build ./src --cjs",
        "build:watch": "tsc -p ./tsconfig.json --module CommonJS --outDir ./cjs --watch",
        "test": "mocha -r @ts-tools/node/r test/**/*.spec.ts --watch-extensions ts --colors"
    },
    "dependencies": {
        "eventemitter3": "^4.0.5"
    },
    "devDependencies": {
        "fast-check": "^2.3.0"
    }
}
